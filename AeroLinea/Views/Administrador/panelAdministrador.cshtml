@{
    Layout = null;
    var usuarios = ViewBag.Usuarios as List<AeroLinea.Models.Usuario>;
    var consultas = ViewBag.Consultas as List<AeroLinea.Models.Consulta>;
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Panel de Administrador</title>
    <link rel="stylesheet" href="~/css/Administrador/panelAdministrador.css" /> 
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">

    <script>
        // JavaScript para controlar el cambio de secciones
        function mostrarSeccion(seccion) {
            var secciones = document.querySelectorAll('.seccion');
            secciones.forEach(function(sec) {
                sec.style.display = 'none';
            });

            document.getElementById(seccion).style.display = 'block';

            if (seccion === 'pilotos') {
                cargarPilotos();
            } else if (seccion === 'flota') {
                cargarFlota();
            } else if (seccion === 'vuelos') {
                cargarVuelos();
                cargarAviones();
                cargarPilotos();
            }
        }

        window.onload = function() {
            mostrarSeccion('usuarios');
        };

        let vueloId = 1;
        function agregarVuelo() {
            const ruta = document.getElementById('ruta').value;
            const fecha = document.getElementById('fecha').value;
            const hora = document.getElementById('hora').value;

            if (ruta === "" || fecha === "" || hora === "") {
                alert("Por favor complete todos los campos.");
                return;
            }
            const nuevaFila = document.createElement('tr');
            const celdaId = document.createElement('td');
            celdaId.textContent = vueloId++;
            const celdaRuta = document.createElement('td');
            celdaRuta.textContent = ruta;
            const celdaFecha = document.createElement('td');
            celdaFecha.textContent = fecha;
            const celdaHora = document.createElement('td');
            celdaHora.textContent = hora;
            const celdaEstado = document.createElement('td');
            celdaEstado.textContent = 'Programado';
            nuevaFila.appendChild(celdaId);
            nuevaFila.appendChild(celdaRuta);
            nuevaFila.appendChild(celdaFecha);
            nuevaFila.appendChild(celdaHora);
            nuevaFila.appendChild(celdaEstado);

            document.getElementById('tabla-vuelos').querySelector('tbody').appendChild(nuevaFila)
            document.getElementById('ruta').value = "";
            document.getElementById('fecha').value = "";
            document.getElementById('hora').value = "";
            alert('Vuelo registrado con éxito!');          

        }

        function mostrarMensajeError(mensaje, elementoId) {
            const elemento = document.getElementById(elementoId);
            elemento.textContent = mensaje;
            elemento.style.display = 'block';
            setTimeout(() => {
                elemento.style.display = 'none';
            }, 5000);
        }

        function mostrarMensajeExito(mensaje) {
            const elemento = document.getElementById('mensajeExito');
            elemento.textContent = mensaje;
            elemento.style.display = 'block';
            setTimeout(() => {
                elemento.style.display = 'none';
            }, 5000);
        }

        function editarUsuario(id) {
            document.getElementById('errorEdicion').style.display = 'none';
            fetch('/Home/ObtenerUsuario/' + id)
                .then(response => response.json())
                .then(usuario => {
                    if (!usuario.success) {
                        mostrarMensajeError(usuario.message || 'Error al cargar usuario', 'errorEdicion');
                        return;
                    }
                    document.getElementById('editId').value = usuario.idUsuario;
                    document.getElementById('editNombres').value = usuario.nombresUsuario;
                    document.getElementById('editApellidos').value = usuario.apellidosUsuario;
                    document.getElementById('editNacimiento').value = usuario.nacimientoUsuario;
                    document.getElementById('editTelefono').value = usuario.telefonoUsuario;
                    document.getElementById('editIdentificacion').value = usuario.identificacionUsuario;
                    document.getElementById('editCorreo').value = usuario.correoUsuario;
                    document.getElementById('editDiscapacidad').value = usuario.discapacidad;
                    $('#modalEditar').modal('show');
                })
                .catch(error => {
                    mostrarMensajeError('Error al cargar usuario: ' + error.message, 'errorEdicion');
                });
        }

        function guardarEdicion() {
            const usuario = {
                idUsuario: document.getElementById('editId').value
            };

            // Solo agregar campos que no estén vacíos
            const nombres = document.getElementById('editNombres').value;
            const apellidos = document.getElementById('editApellidos').value;
            const nacimiento = document.getElementById('editNacimiento').value;
            const telefono = document.getElementById('editTelefono').value;
            const identificacion = document.getElementById('editIdentificacion').value;
            const correo = document.getElementById('editCorreo').value;
            const discapacidad = document.getElementById('editDiscapacidad').value;

            if (nombres) usuario.nombresUsuario = nombres;
            if (apellidos) usuario.apellidosUsuario = apellidos;
            if (nacimiento) usuario.nacimientoUsuario = nacimiento;
            if (telefono) usuario.telefonoUsuario = telefono;
            if (identificacion) usuario.identificacionUsuario = identificacion;
            if (correo) usuario.correoUsuario = correo;
            usuario.discapacidad = discapacidad === 'true';

            fetch('/Home/ActualizarUsuario', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify(usuario)
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    $('#modalEditar').modal('hide');
                    mostrarMensajeExito(data.message);
                    location.reload();
                } else {
                    mostrarMensajeError(data.message || 'Error al actualizar usuario', 'errorEdicion');
                }
            })
            .catch(error => {
                mostrarMensajeError('Error al actualizar usuario: ' + error.message, 'errorEdicion');
            });
        }

        function cambiarRol(id, esAdmin) {
            document.getElementById('errorRol').style.display = 'none';
            document.getElementById('cambiarRolId').value = id;
            document.getElementById('cambiarRolActual').value = esAdmin;
            $('#modalCambiarRol').modal('show');
        }

        function confirmarCambioRol() {
            const id = document.getElementById('cambiarRolId').value;
            const esAdmin = document.getElementById('cambiarRolActual').value === 'true';

            fetch('/Home/CambiarRol', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                    id: id,
                    esAdmin: !esAdmin
                })
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    $('#modalCambiarRol').modal('hide');
                    mostrarMensajeExito(data.message || 'Rol actualizado correctamente');
                    location.reload();
                } else {
                    mostrarMensajeError(data.message || 'Error al cambiar el rol', 'errorRol');
                }
            })
            .catch(error => {
                mostrarMensajeError('Error al cambiar rol: ' + error.message, 'errorRol');
            });
        }

        function eliminarUsuario(id) {
            document.getElementById('errorEliminar').style.display = 'none';
            document.getElementById('eliminarId').value = id;
            $('#modalEliminar').modal('show');
        }

        function confirmarEliminacion() {
            const id = document.getElementById('eliminarId').value;

            fetch('/Home/EliminarUsuario', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                    id: id
                })
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    $('#modalEliminar').modal('hide');
                    mostrarMensajeExito(data.message || 'Usuario eliminado correctamente');
                    location.reload();
                } else {
                    mostrarMensajeError(data.message || 'Error al eliminar usuario', 'errorEliminar');
                }
            })
            .catch(error => {
                mostrarMensajeError('Error al eliminar usuario: ' + error.message, 'errorEliminar');
            });
        }

        function marcarResuelto(id) {
            fetch('/Home/ActualizarEstadoConsulta', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                    id: id,
                    estado: 'Resuelto'
                })
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    mostrarMensajeExito('Consulta marcada como resuelta');
                    location.reload();
                } else {
                    mostrarMensajeError(data.message || 'Error al actualizar estado', 'mensajeErrorConsultas');
                }
            })
            .catch(error => {
                mostrarMensajeError('Error al actualizar estado: ' + error.message, 'mensajeErrorConsultas');
            });
        }

        function marcarPendiente(id) {
            fetch('/Home/ActualizarEstadoConsulta', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                    id: id,
                    estado: 'Pendiente'
                })
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    mostrarMensajeExito('Consulta marcada como pendiente');
                    location.reload();
                } else {
                    mostrarMensajeError(data.message || 'Error al actualizar estado', 'mensajeErrorConsultas');
                }
            })
            .catch(error => {
                mostrarMensajeError('Error al actualizar estado: ' + error.message, 'mensajeErrorConsultas');
            });
        }

        function editarPiloto(idPiloto) {
            fetch(`/Home/ObtenerPiloto/${idPiloto}`)
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        document.getElementById('nombrePiloto').value = data.data.nombrePiloto;
                        document.getElementById('apellidoPiloto').value = data.data.apellidoPiloto;
                        document.getElementById('nacimientoPiloto').value = data.data.nacimientoPiloto.split('T')[0];
                        document.getElementById('telefonoPiloto').value = data.data.telefonoPiloto;
                        document.getElementById('dniPiloto').value = data.data.dniPiloto;
                        document.getElementById('licenciaPiloto').value = data.data.licenciaPiloto;
                        document.getElementById('tipoLicPiloto').value = data.data.tipoLicPiloto;
                        document.getElementById('fechaEmiLic').value = data.data.fechaEmiLic.split('T')[0];
                        document.querySelector('#modalRegistrarPiloto .modal-title').textContent = 'Editar Piloto';
                        document.querySelector('#modalRegistrarPiloto .btn-primary').textContent = 'Guardar Cambios';
                        document.querySelector('#modalRegistrarPiloto').dataset.pilotoId = idPiloto;
                        $('#modalRegistrarPiloto').modal('show');
                    } else {
                        alert(data.message || 'Error al cargar datos del piloto');
                    }
                })
                .catch(error => {
                    alert('Error al cargar datos del piloto: ' + error.message);
                });
        }

        function eliminarPiloto(idPiloto) {
            document.getElementById('eliminarPilotoId').value = idPiloto;
            $('#modalEliminarPiloto').modal('show');
        }

        function confirmarEliminacionPiloto() {
            const idPiloto = document.getElementById('eliminarPilotoId').value;
            fetch(`/Home/EliminarPiloto/${idPiloto}`, {
                method: 'POST'
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    $('#modalEliminarPiloto').modal('hide');
                    mostrarMensajeExito('Piloto eliminado exitosamente');
                    cargarPilotos();
                } else {
                    mostrarMensajeError(data.message || 'Error al eliminar piloto', 'errorEliminarPiloto');
                }
            })
            .catch(error => {
                mostrarMensajeError('Error al eliminar piloto: ' + error.message, 'errorEliminarPiloto');
            });
        }

        function registrarPiloto() {
            // Limpiar mensajes de error anteriores
            document.querySelectorAll('.error-message').forEach(el => el.remove());
            
            const pilotoId = document.querySelector('#modalRegistrarPiloto').dataset.pilotoId;
            const piloto = {
                nombrePiloto: document.getElementById('nombrePiloto').value,
                apellidoPiloto: document.getElementById('apellidoPiloto').value,
                nacimientoPiloto: document.getElementById('nacimientoPiloto').value,
                telefonoPiloto: document.getElementById('telefonoPiloto').value,
                dniPiloto: document.getElementById('dniPiloto').value,
                licenciaPiloto: document.getElementById('licenciaPiloto').value,
                tipoLicPiloto: document.getElementById('tipoLicPiloto').value,
                fechaEmiLic: document.getElementById('fechaEmiLic').value
            };

            const url = pilotoId ? `/Home/ActualizarPiloto/${pilotoId}` : '/Home/RegistrarPiloto';
            const method = pilotoId ? 'PUT' : 'POST';

            fetch(url, {
                method: method,
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(piloto)
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    mostrarMensajeExito(pilotoId ? 'Piloto actualizado exitosamente' : 'Piloto registrado exitosamente', 'mensajeExitoPiloto');
                    // Limpiar los campos del formulario
                    document.getElementById('nombrePiloto').value = '';
                    document.getElementById('apellidoPiloto').value = '';
                    document.getElementById('nacimientoPiloto').value = '';
                    document.getElementById('telefonoPiloto').value = '';
                    document.getElementById('dniPiloto').value = '';
                    document.getElementById('licenciaPiloto').value = '';
                    document.getElementById('tipoLicPiloto').value = '';
                    document.getElementById('fechaEmiLic').value = '';
                    // Restaurar el título y el botón
                    document.querySelector('#modalRegistrarPiloto .modal-title').textContent = 'Registrar Piloto';
                    document.querySelector('#modalRegistrarPiloto .btn-primary').textContent = 'Registrar';
                    // Limpiar el ID del piloto
                    document.querySelector('#modalRegistrarPiloto').dataset.pilotoId = '';
                    $('#modalRegistrarPiloto').modal('hide');
                    cargarPilotos();
                } else {
                    if (data.errors) {
                        // Mostrar errores de validación debajo de cada campo
                        Object.keys(data.errors).forEach(key => {
                            const input = document.getElementById(key);
                            if (input) {
                                const errorDiv = document.createElement('div');
                                errorDiv.className = 'error-message text-danger mt-1';
                                errorDiv.textContent = data.errors[key];
                                input.parentNode.appendChild(errorDiv);
                            }
                        });
                    } else {
                        mostrarMensajeError(data.message || 'Error al procesar la solicitud', 'errorRegistroPiloto');
                    }
                }
            })
            .catch(error => {
                mostrarMensajeError('Error al procesar la solicitud: ' + error.message, 'errorRegistroPiloto');
            });
        }

        function cargarPilotos() {
            console.log('Iniciando carga de pilotos...');
            fetch('/Home/ObtenerPilotos')
                .then(response => {
                    console.log('Respuesta del servidor:', response);
                    return response.json();
                })
                .then(data => {
                    console.log("Pilotos recibidos:", data);
                    if (data.success) {
                        // Cargar la tabla de pilotos
                        const tbody = document.querySelector('#pilotos table tbody');
                        if (tbody) {
                            tbody.innerHTML = '';
                            data.data.forEach(piloto => {
                                const tr = document.createElement('tr');
                                tr.innerHTML = `
                                    <td>${piloto.idPiloto}</td>
                                    <td>${piloto.nombrePiloto}</td>
                                    <td>${piloto.apellidoPiloto}</td>
                                    <td>${new Date(piloto.nacimientoPiloto).toLocaleDateString()}</td>
                                    <td>${piloto.telefonoPiloto}</td>
                                    <td>${piloto.dniPiloto}</td>
                                    <td>${piloto.licenciaPiloto}</td>
                                    <td>${piloto.tipoLicPiloto}</td>
                                    <td>${new Date(piloto.fechaEmiLic).toLocaleDateString()}</td>
                                    <td>
                                        <button class="btn btn-sm btn-primary" onclick="editarPiloto(${piloto.idPiloto})">
                                            <i class="fas fa-edit"></i>
                                        </button>
                                        <button class="btn btn-sm btn-danger" onclick="eliminarPiloto(${piloto.idPiloto})">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </td>
                                `;
                                tbody.appendChild(tr);
                            });
                        }

                        // Cargar el selector de pilotos en el modal de vuelos
                        const select = document.getElementById('idPiloto');
                        console.log('Selector de pilotos encontrado:', select);
                        if (select) {
                            select.innerHTML = '<option value="">Seleccione un piloto</option>';
                            data.data.forEach(piloto => {
                                const option = document.createElement('option');
                                option.value = piloto.idPiloto;
                                option.textContent = `${piloto.nombrePiloto} ${piloto.apellidoPiloto} - ${piloto.dniPiloto}`;
                                select.appendChild(option);
                            });
                            console.log('Pilotos cargados en el selector:', select.options.length);
                        } else {
                            console.error('No se encontró el selector de pilotos');
                        }
                    } else {
                        console.error('Error al cargar pilotos:', data.message);
                        mostrarMensajeError(data.message || 'Error al cargar pilotos', 'mensajeErrorPilotos');
                    }
                })
                .catch(error => {
                    console.error('Error al cargar pilotos:', error);
                    mostrarMensajeError('Error al cargar pilotos: ' + error.message, 'mensajeErrorPilotos');
                });
        }

        function cargarFlota() {
            fetch('/Home/ObtenerFlota')
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        const tbody = document.querySelector('#flota table tbody');
                        tbody.innerHTML = '';
                        data.data.forEach(avion => {
                            const tr = document.createElement('tr');
                            tr.innerHTML = `
                                <td>${avion.idAvion}</td>
                                <td>${avion.modeloAvion}</td>
                                <td>${avion.fabricanteAvion}</td>
                                <td>${avion.matriculaAvion}</td>
                                <td>${avion.capacidadAvion}</td>
                                <td>${avion.claseAvion}</td>
                                <td>
                                    <button class="btn btn-sm btn-primary" onclick="editarAvion(${avion.idAvion})">
                                        <i class="fas fa-edit"></i>
                                    </button>
                                    <button class="btn btn-sm btn-danger" onclick="eliminarAvion(${avion.idAvion})">
                                        <i class="fas fa-trash"></i>
                                    </button>
                                </td>
                            `;
                            tbody.appendChild(tr);
                        });
                    } else {
                        mostrarMensajeError(data.message || 'Error al cargar flota', 'mensajeErrorFlota');
                    }
                })
                .catch(error => {
                    mostrarMensajeError('Error al cargar flota: ' + error.message, 'mensajeErrorFlota');
                });
        }

        function registrarAvion() {
            // Limpiar mensajes de error anteriores
            document.querySelectorAll('.error-message').forEach(el => el.remove());
            
            const avion = {
                modeloAvion: document.getElementById('modeloAvion').value,
                fabricanteAvion: document.getElementById('fabricanteAvion').value,
                matriculaAvion: document.getElementById('matriculaAvion').value,
                capacidadAvion: parseInt(document.getElementById('capacidadAvion').value),
                claseAvion: document.getElementById('claseAvion').value
            };

            fetch('/Home/RegistrarAvion', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(avion)
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    $('#modalRegistrarAvion').modal('hide');
                    mostrarMensajeExito('Avión registrado exitosamente');
                    document.getElementById('formRegistrarAvion').reset();
                    cargarFlota();
                } else {
                    if (data.errors) {
                        // Mostrar errores de validación debajo de cada campo
                        Object.keys(data.errors).forEach(key => {
                            const input = document.getElementById(key);
                            if (input) {
                                const errorDiv = document.createElement('div');
                                errorDiv.className = 'error-message text-danger mt-1';
                                errorDiv.textContent = data.errors[key];
                                input.parentNode.appendChild(errorDiv);
                            }
                        });
                    } else {
                        mostrarMensajeError(data.message || 'Error al registrar avión', 'errorRegistroAvion');
                    }
                }
            })
            .catch(error => {
                mostrarMensajeError('Error al registrar avión: ' + error.message, 'errorRegistroAvion');
            });
        }

        function editarAvion(idAvion) {
            fetch(`/Home/ObtenerAvion/${idAvion}`)
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        document.getElementById('modeloAvion').value = data.data.modeloAvion;
                        document.getElementById('fabricanteAvion').value = data.data.fabricanteAvion;
                        document.getElementById('matriculaAvion').value = data.data.matriculaAvion;
                        document.getElementById('capacidadAvion').value = data.data.capacidadAvion;
                        document.getElementById('claseAvion').value = data.data.claseAvion;
                        document.querySelector('#modalRegistrarAvion').dataset.avionId = idAvion;
                        document.querySelector('#modalRegistrarAvion .modal-title').textContent = 'Editar Avión';
                        document.querySelector('#modalRegistrarAvion .btn-primary').textContent = 'Guardar Cambios';
                        $('#modalRegistrarAvion').modal('show');
                    } else {
                        mostrarMensajeError(data.message || 'Error al cargar datos del avión', 'errorRegistroAvion');
                    }
                })
                .catch(error => {
                    mostrarMensajeError('Error al cargar datos del avión: ' + error.message, 'errorRegistroAvion');
                });
        }

        function eliminarAvion(idAvion) {
            document.getElementById('eliminarAvionId').value = idAvion;
            $('#modalEliminarAvion').modal('show');
        }

        function confirmarEliminacionAvion() {
            const idAvion = document.getElementById('eliminarAvionId').value;
            fetch(`/Home/EliminarAvion/${idAvion}`, {
                method: 'POST'
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    $('#modalEliminarAvion').modal('hide');
                    mostrarMensajeExito('Avión eliminado exitosamente');
                    cargarFlota();
                } else {
                    mostrarMensajeError(data.message || 'Error al eliminar avión', 'errorEliminarAvion');
                }
            })
            .catch(error => {
                mostrarMensajeError('Error al eliminar avión: ' + error.message, 'errorEliminarAvion');
            });
        }

        function cargarVuelos() {
            fetch('/Home/ObtenerVuelos')
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        const tbody = document.querySelector('#vuelos table tbody');
                        tbody.innerHTML = '';
                        data.data.forEach(vuelo => {
                            const tr = document.createElement('tr');
                            tr.innerHTML = `
                                <td>${vuelo.idVuelo}</td>
                                <td>${vuelo.origenVuelo}</td>
                                <td>${vuelo.destinoVuelo}</td>
                                <td>${new Date(vuelo.fechaVuelo).toLocaleDateString()}</td>
                                <td>${vuelo.avion.matriculaAvion}</td>
                                <td>${vuelo.piloto.nombrePiloto} ${vuelo.piloto.apellidoPiloto}</td>
                                <td>${vuelo.precioVuelo}</td>
                                <td>
                                    <button class="btn btn-sm btn-primary" onclick="editarVuelo(${vuelo.idVuelo})">
                                        <i class="fas fa-edit"></i>
                                    </button>
                                    <button class="btn btn-sm btn-danger" onclick="eliminarVuelo(${vuelo.idVuelo})">
                                        <i class="fas fa-trash"></i>
                                    </button>
                                </td>
                            `;
                            tbody.appendChild(tr);
                        });
                    } else {
                        mostrarMensajeError(data.message || 'Error al cargar vuelos', 'mensajeErrorVuelos');
                    }
                })
                .catch(error => {
                    mostrarMensajeError('Error al cargar vuelos: ' + error.message, 'mensajeErrorVuelos');
                });
        }

        function cargarAviones() {
            fetch('/Home/ObtenerFlota')
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        const select = document.getElementById('idAvion');
                        select.innerHTML = '<option value="">Seleccione un avión</option>';
                        data.data.forEach(avion => {
                            const option = document.createElement('option');
                            option.value = avion.idAvion;
                            option.textContent = `${avion.matriculaAvion} - ${avion.modeloAvion}`;
                            select.appendChild(option);
                        });
                    }
                });
        }

        function registrarVuelo() {
            // Limpiar mensajes de error anteriores
            document.querySelectorAll('.text-danger').forEach(el => el.style.display = 'none');
            
            const origen = document.getElementById('origenVuelo').value;
            const destino = document.getElementById('destinoVuelo').value;
            const fecha = document.getElementById('fechaVuelo').value;
            const precio = document.getElementById('precioVuelo').value;
            const idAvion = document.getElementById('idAvion').value;
            const idPiloto = document.getElementById('idPiloto').value;

            let hayErrores = false;

            // Validaciones individuales
            if (!origen) {
                document.getElementById('errorOrigen').textContent = 'El origen es obligatorio';
                document.getElementById('errorOrigen').style.display = 'block';
                hayErrores = true;
            }

            if (!destino) {
                document.getElementById('errorDestino').textContent = 'El destino es obligatorio';
                document.getElementById('errorDestino').style.display = 'block';
                hayErrores = true;
            }

            if (!fecha) {
                document.getElementById('errorFecha').textContent = 'La fecha es obligatoria';
                document.getElementById('errorFecha').style.display = 'block';
                hayErrores = true;
            }

            if (!precio) {
                document.getElementById('errorPrecio').textContent = 'El precio es obligatorio';
                document.getElementById('errorPrecio').style.display = 'block';
                hayErrores = true;
            }

            if (hayErrores) return;

            const vuelo = {
                origenVuelo: origen,
                destinoVuelo: destino,
                fechaVuelo: fecha,
                precioVuelo: parseFloat(precio),
                idAvion: idAvion ? parseInt(idAvion) : null,
                idPiloto: idPiloto ? parseInt(idPiloto) : null
            };

            fetch('/Home/RegistrarVuelo', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(vuelo)
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    document.getElementById('formVuelo').reset();
                    document.querySelector('#modalRegistrarVuelo').modal('hide');
                    cargarVuelos();
                } else {
                    if (data.errors) {
                        Object.keys(data.errors).forEach(key => {
                            const errorElement = document.getElementById(`error${key.charAt(0).toUpperCase() + key.slice(1)}`);
                            if (errorElement) {
                                errorElement.textContent = data.errors[key];
                                errorElement.style.display = 'block';
                            }
                        });
                    } else {
                        document.getElementById('errorGeneral').textContent = data.message;
                        document.getElementById('errorGeneral').style.display = 'block';
                    }
                }
            })
            .catch(error => {
                console.error('Error:', error);
                document.getElementById('errorGeneral').textContent = 'Error al registrar el vuelo';
                document.getElementById('errorGeneral').style.display = 'block';
            });
        }

        function editarVuelo(idVuelo) {
            fetch(`/Home/ObtenerVuelo/${idVuelo}`)
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        document.getElementById('origenVuelo').value = data.data.origenVuelo;
                        document.getElementById('destinoVuelo').value = data.data.destinoVuelo;
                        document.getElementById('fechaVuelo').value = data.data.fechaVuelo.split('T')[0];
                        document.getElementById('idAvion').value = data.data.idAvion;
                        document.getElementById('idPiloto').value = data.data.idPiloto;
                        document.getElementById('precioVuelo').value = data.data.precioVuelo;
                        document.querySelector('#modalRegistrarVuelo').dataset.vueloId = idVuelo;
                        document.querySelector('#modalRegistrarVuelo .modal-title').textContent = 'Editar Vuelo';
                        document.querySelector('#modalRegistrarVuelo .btn-primary').textContent = 'Guardar Cambios';
                        $('#modalRegistrarVuelo').modal('show');
                    } else {
                        mostrarMensajeError(data.message || 'Error al cargar datos del vuelo', 'errorRegistroVuelo');
                    }
                })
                .catch(error => {
                    mostrarMensajeError('Error al cargar datos del vuelo: ' + error.message, 'errorRegistroVuelo');
                });
        }

        function eliminarVuelo(idVuelo) {
            document.getElementById('eliminarVueloId').value = idVuelo;
            $('#modalEliminarVuelo').modal('show');
        }

        function confirmarEliminacionVuelo() {
            const idVuelo = document.getElementById('eliminarVueloId').value;
            fetch(`/Home/EliminarVuelo/${idVuelo}`, {
                method: 'POST'
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    $('#modalEliminarVuelo').modal('hide');
                    mostrarMensajeExito('Vuelo eliminado exitosamente');
                    cargarVuelos();
                } else {
                    mostrarMensajeError(data.message || 'Error al eliminar vuelo', 'errorEliminarVuelo');
                }
            })
            .catch(error => {
                mostrarMensajeError('Error al eliminar vuelo: ' + error.message, 'errorEliminarVuelo');
            });
        }

        // Cargar pilotos y aviones al abrir el modal de registro de vuelo
        $('#modalRegistrarVuelo').on('show.bs.modal', function () {
            console.log('Modal de vuelos abierto, cargando pilotos...');
            cargarPilotos();
            cargarAviones();
        });
    </script>
    
</head>
<body>
    <div class="admin-panel">
        <!-- Barra lateral -->
        <nav class="sidebar">
            <div class="sidebar-header">
                <h2>Admin Panel</h2>
            </div>
            <ul class="sidebar-menu">
                <li><a href="javascript:void(0);" onclick="mostrarSeccion('usuarios')">Usuarios</a></li>
                <li><a href="javascript:void(0);" onclick="mostrarSeccion('consultas')">Consultas</a></li>
                <li><a href="javascript:void(0);" onclick="mostrarSeccion('pilotos')">Pilotos</a></li>
                <li><a href="javascript:void(0);" onclick="mostrarSeccion('flota')">Flota</a></li>
                <li><a href="javascript:void(0);" onclick="mostrarSeccion('vuelos')">Vuelos</a></li>
                <li><a href="@Url.Action("Index", "Home")">Volver al Inicio</a></li>
            </ul>
        </nav>

        <!-- Contenido principal -->
        <div class="main-content">
            <div id="contenido">
                <!-- Sección Usuarios -->
                <section id="usuarios" class="seccion">
                    <h3>Usuarios Registrados</h3>
                    <div id="mensajeError" class="alert alert-danger" style="display: none;"></div>
                    <div id="mensajeExito" class="alert alert-success" style="display: none;"></div>
                    <div class="table-responsive">
                        <table class="table">
                            <thead class="thead-dark">
                                <tr>
                                    <th>Nombres</th>
                                    <th>Apellidos</th>
                                    <th>Nacimiento</th>
                                    <th>Teléfono</th>
                                    <th>Identificación</th>
                                    <th>Correo</th>
                                    <th>Contraseña</th>
                                    <th>Discapacidad</th>
                                    <th>Rol</th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var usuario in usuarios)
                                {
                                    <tr>
                                        <td>@usuario.nombresUsuario</td>
                                        <td>@usuario.apellidosUsuario</td>
                                        <td>@usuario.nacimientoUsuario.ToShortDateString()</td>
                                        <td>@usuario.telefonoUsuario</td>
                                        <td>@usuario.identificacionUsuario</td>
                                        <td>@usuario.correoUsuario</td>
                                        <td>********</td>
                                        <td>@(usuario.discapacidad ? "Sí" : "No")</td>
                                        <td>@(usuario.esAdmin ? "Administrador" : "Pasajero")</td>
                                        <td>
                                            <button class="btn btn-sm btn-primary" onclick="editarUsuario(@usuario.idUsuario)">
                                                <i class="fas fa-edit"></i>
                                            </button>
                                            <button class="btn btn-sm btn-warning" onclick="cambiarRol(@usuario.idUsuario, @usuario.esAdmin.ToString().ToLower())">
                                                <i class="fas fa-user-shield"></i>
                                            </button>
                                            <button class="btn btn-sm btn-danger" onclick="eliminarUsuario(@usuario.idUsuario)">
                                                <i class="fas fa-trash"></i>
                                            </button>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </section>

                <!-- Sección Consultas -->
                <section id="consultas" class="seccion" style="display: none;">
                    <h3>Consultas de Clientes</h3>
                    <div id="mensajeErrorConsultas" class="alert alert-danger" style="display: none;"></div>
                    <div id="mensajeExitoConsultas" class="alert alert-success" style="display: none;"></div>
                    <div class="table-responsive">
                        <table class="table">
                            <thead class="thead-dark">
                                <tr>
                                    <th>ID</th>
                                    <th>Usuario</th>
                                    <th>Motivo</th>
                                    <th>Descripción</th>
                                    <th>Urgencia</th>
                                    <th>Estado</th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var consulta in consultas)
                                {
                                    <tr class="@(consulta.urgencia == "Alto" ? "table-danger" : consulta.urgencia == "Medio" ? "table-warning" : "table-info")">
                                        <td>@consulta.idConsulta</td>
                                        <td>@consulta.Usuario.nombresUsuario @consulta.Usuario.apellidosUsuario</td>
                                        <td>@consulta.motivoConsulta</td>
                                        <td>@consulta.descripcionConsultas</td>
                                        <td>@consulta.urgencia</td>
                                        <td>
                                            <span class="badge @(consulta.estado == "Resuelto" ? "bg-success" : "bg-warning")">
                                                @consulta.estado
                                            </span>
                                        </td>
                                        <td>
                                            <button class="btn btn-sm btn-success" onclick="marcarResuelto(@consulta.idConsulta)">
                                                <i class="fas fa-check"></i>
                                            </button>
                                            <button class="btn btn-sm btn-warning" onclick="marcarPendiente(@consulta.idConsulta)">
                                                <i class="fas fa-clock"></i>
                                            </button>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </section>

                <!-- Sección Pilotos -->
                <section id="pilotos" class="seccion" style="display: none;">
                    <h3>Registro de Pilotos</h3>
                    <div id="mensajeErrorPilotos" class="alert alert-danger" style="display: none;"></div>
                    <div id="mensajeExitoPilotos" class="alert alert-success" style="display: none;"></div>
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Nombre</th>
                                    <th>Apellido</th>
                                    <th>Fecha de Nacimiento</th>
                                    <th>Teléfono</th>
                                    <th>DNI</th>
                                    <th>Licencia</th>
                                    <th>Tipo de Licencia</th>
                                    <th>Fecha de Emisión</th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                <!-- Los datos se cargarán dinámicamente -->
                            </tbody>
                        </table>
                    </div>
                    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#modalRegistrarPiloto">
                        <i class="fas fa-plus"></i> Registrar Piloto
                    </button>
                </section>

                <!-- Sección Flota -->
                <section id="flota" class="seccion" style="display: none;">
                    <h3>Gestión de Flota</h3>
                    <div id="mensajeErrorFlota" class="alert alert-danger" style="display: none;"></div>
                    <div id="mensajeExitoFlota" class="alert alert-success" style="display: none;"></div>
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Modelo</th>
                                    <th>Fabricante</th>
                                    <th>Matrícula</th>
                                    <th>Capacidad</th>
                                    <th>Clase</th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                <!-- Los datos se cargarán dinámicamente -->
                            </tbody>
                        </table>
                    </div>
                    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#modalRegistrarAvion">
                        <i class="fas fa-plus"></i> Registrar Avión
                    </button>
                </section>

                <!-- Sección Vuelos -->
                <section id="vuelos" class="seccion" style="display: none;">
                    <h3>Gestión de Vuelos</h3>
                    <div id="mensajeErrorVuelos" class="alert alert-danger" style="display: none;"></div>
                    <div id="mensajeExitoVuelos" class="alert alert-success" style="display: none;"></div>
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Origen</th>
                                    <th>Destino</th>
                                    <th>Fecha</th>
                                    <th>Avión</th>
                                    <th>Piloto</th>
                                    <th>Precio</th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                <!-- Los datos se cargarán dinámicamente -->
                            </tbody>
                        </table>
                    </div>
                    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#modalRegistrarVuelo">
                        <i class="fas fa-plus"></i> Registrar Vuelo
                    </button>
                </section>
            </div>
        </div>
    </div>

    <!-- Modal Editar Usuario -->
    <div class="modal fade" id="modalEditar" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Editar Usuario</h5>
                    <button type="button" class="close" data-dismiss="modal">
                        <span>&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div id="errorEdicion" class="alert alert-danger" style="display: none;"></div>
                    <input type="hidden" id="editId">
                    <div class="form-group">
                        <label>Nombres</label>
                        <input type="text" class="form-control" id="editNombres" required>
                    </div>
                    <div class="form-group">
                        <label>Apellidos</label>
                        <input type="text" class="form-control" id="editApellidos" required>
                    </div>
                    <div class="form-group">
                        <label>Fecha de Nacimiento</label>
                        <input type="date" class="form-control" id="editNacimiento" required>
                    </div>
                    <div class="form-group">
                        <label>Teléfono</label>
                        <input type="text" class="form-control" id="editTelefono" required>
                    </div>
                    <div class="form-group">
                        <label>Identificación</label>
                        <input type="text" class="form-control" id="editIdentificacion" required>
                    </div>
                    <div class="form-group">
                        <label>Correo</label>
                        <input type="email" class="form-control" id="editCorreo" required>
                    </div>
                    <div class="form-group">
                        <label>Discapacidad</label>
                        <select class="form-control" id="editDiscapacidad">
                            <option value="false">No</option>
                            <option value="true">Sí</option>
                        </select>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-primary" onclick="guardarEdicion()">Guardar Cambios</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal Cambiar Rol -->
    <div class="modal fade" id="modalCambiarRol" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Cambiar Rol de Usuario</h5>
                    <button type="button" class="close" data-dismiss="modal">
                        <span>&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div id="errorRol" class="alert alert-danger" style="display: none;"></div>
                    <input type="hidden" id="cambiarRolId">
                    <input type="hidden" id="cambiarRolActual">
                    <p>¿Está seguro que desea cambiar el rol de este usuario?</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-warning" onclick="confirmarCambioRol()">Confirmar Cambio</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal Eliminar Usuario -->
    <div class="modal fade" id="modalEliminar" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Eliminar Usuario</h5>
                    <button type="button" class="close" data-dismiss="modal">
                        <span>&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div id="errorEliminar" class="alert alert-danger" style="display: none;"></div>
                    <input type="hidden" id="eliminarId">
                    <p>¿Está seguro que desea eliminar este usuario?</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-danger" onclick="confirmarEliminacion()">Confirmar Eliminación</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal Registrar Piloto -->
    <div class="modal fade" id="modalRegistrarPiloto" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Registrar Nuevo Piloto</h5>
                    <button type="button" class="close" data-dismiss="modal">
                        <span>&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div id="errorRegistroPiloto" class="alert alert-danger" style="display: none;"></div>
                    <form id="formRegistrarPiloto">
                        <div class="form-group">
                            <label>Nombre</label>
                            <input type="text" class="form-control" id="nombrePiloto" required maxlength="30">
                        </div>
                        <div class="form-group">
                            <label>Apellido</label>
                            <input type="text" class="form-control" id="apellidoPiloto" required maxlength="30">
                        </div>
                        <div class="form-group">
                            <label>Fecha de Nacimiento</label>
                            <input type="date" class="form-control" id="nacimientoPiloto" required>
                        </div>
                        <div class="form-group">
                            <label>Teléfono</label>
                            <input type="number" class="form-control" id="telefonoPiloto" required>
                        </div>
                        <div class="form-group">
                            <label>DNI</label>
                            <input type="text" class="form-control" id="dniPiloto" required maxlength="8">
                        </div>
                        <div class="form-group">
                            <label>Licencia de Piloto</label>
                            <input type="text" class="form-control" id="licenciaPiloto" required maxlength="9">
                        </div>
                        <div class="form-group">
                            <label>Tipo de Licencia</label>
                            <select class="form-control" id="tipoLicPiloto" required>
                                <option value="">Seleccione un tipo</option>
                                <option value="Privado">Privado</option>
                                <option value="Comercial">Comercial</option>
                                <option value="Ligeras">Ligeras</option>
                                <option value="Linea Aerea">Línea Aérea</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label>Fecha de Emisión de Licencia</label>
                            <input type="date" class="form-control" id="fechaEmiLic" required>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-primary" onclick="registrarPiloto()">Registrar Piloto</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal Eliminar Piloto -->
    <div class="modal fade" id="modalEliminarPiloto" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Eliminar Piloto</h5>
                    <button type="button" class="close" data-dismiss="modal">
                        <span>&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div id="errorEliminarPiloto" class="alert alert-danger" style="display: none;"></div>
                    <input type="hidden" id="eliminarPilotoId">
                    <p>¿Está seguro que desea eliminar este piloto?</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-danger" onclick="confirmarEliminacionPiloto()">Confirmar Eliminación</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal Registrar Avión -->
    <div class="modal fade" id="modalRegistrarAvion" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Registrar Nuevo Avión</h5>
                    <button type="button" class="close" data-dismiss="modal">
                        <span>&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div id="errorRegistroAvion" class="alert alert-danger" style="display: none;"></div>
                    <form id="formRegistrarAvion">
                        <div class="form-group">
                            <label>Modelo</label>
                            <input type="text" class="form-control" id="modeloAvion" required maxlength="15" minlength="5">
                        </div>
                        <div class="form-group">
                            <label>Fabricante</label>
                            <input type="text" class="form-control" id="fabricanteAvion" required maxlength="10">
                        </div>
                        <div class="form-group">
                            <label>Matrícula</label>
                            <input type="text" class="form-control" id="matriculaAvion" required maxlength="8" minlength="3">
                        </div>
                        <div class="form-group">
                            <label>Capacidad</label>
                            <input type="number" class="form-control" id="capacidadAvion" required min="10" max="200">
                        </div>
                        <div class="form-group">
                            <label>Clase</label>
                            <select class="form-control" id="claseAvion" required>
                                <option value="">Seleccione una clase</option>
                                <option value="Economica">Económica</option>
                                <option value="Ejecutiva">Ejecutiva</option>
                                <option value="Primera clase">Primera clase</option>
                            </select>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-primary" onclick="registrarAvion()">Registrar Avión</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal Eliminar Avión -->
    <div class="modal fade" id="modalEliminarAvion" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Eliminar Avión</h5>
                    <button type="button" class="close" data-dismiss="modal">
                        <span>&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div id="errorEliminarAvion" class="alert alert-danger" style="display: none;"></div>
                    <input type="hidden" id="eliminarAvionId">
                    <p>¿Está seguro que desea eliminar este avión?</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-danger" onclick="confirmarEliminacionAvion()">Confirmar Eliminación</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal Registrar Vuelo -->
    <div class="modal fade" id="modalRegistrarVuelo" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Registrar Nuevo Vuelo</h5>
                    <button type="button" class="close" data-dismiss="modal">
                        <span>&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div id="errorRegistroVuelo" class="alert alert-danger" style="display: none;"></div>
                    <form id="formRegistrarVuelo">
                        <div class="form-group">
                            <label for="origenVuelo">Origen</label>
                            <select class="form-control" id="origenVuelo" name="origenVuelo" required>
                                <option value="">Seleccione una ciudad</option>
                                <option value="Chiclayo">Chiclayo</option>
                                <option value="Cajamarca">Cajamarca</option>
                                <option value="Huanuco">Huanuco</option>
                                <option value="Lima">Lima</option>
                                <option value="Iquitos">Iquitos</option>
                                <option value="Tarapoto">Tarapoto</option>
                                <option value="Pucallpa">Pucallpa</option>
                            </select>
                            <small class="text-danger" id="errorOrigen" style="display: none;"></small>
                        </div>
                        <div class="form-group">
                            <label for="destinoVuelo">Destino</label>
                            <select class="form-control" id="destinoVuelo" name="destinoVuelo" required>
                                <option value="">Seleccione una ciudad</option>
                                <option value="Chiclayo">Chiclayo</option>
                                <option value="Cajamarca">Cajamarca</option>
                                <option value="Huanuco">Huanuco</option>
                                <option value="Lima">Lima</option>
                                <option value="Iquitos">Iquitos</option>
                                <option value="Tarapoto">Tarapoto</option>
                                <option value="Pucallpa">Pucallpa</option>
                            </select>
                            <small class="text-danger" id="errorDestino" style="display: none;"></small>
                        </div>
                        <div class="form-group">
                            <label for="fechaVuelo">Fecha</label>
                            <input type="datetime-local" class="form-control" id="fechaVuelo" required>
                            <small class="text-danger" id="errorFecha" style="display: none;"></small>
                        </div>
                        <div class="form-group">
                            <label for="precioVuelo">Precio</label>
                            <input type="number" class="form-control" id="precioVuelo" required>
                            <small class="text-danger" id="errorPrecio" style="display: none;"></small>
                        </div>
                        <div class="form-group">
                            <label for="idAvion">Avión</label>
                            <select class="form-control" id="idAvion">
                                <option value="">Seleccione un avión</option>
                            </select>
                            <small class="text-danger" id="errorAvion" style="display: none;"></small>
                        </div>
                        <div class="form-group">
                            <label for="idPiloto">Piloto</label>
                            <select class="form-control" id="idPiloto">
                                <option value="">Seleccione un piloto</option>
                            </select>
                            <small class="text-danger" id="errorPiloto" style="display: none;"></small>
                        </div>
                        <small class="text-danger" id="errorGeneral" style="display: none;"></small>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-primary" onclick="registrarVuelo()">Registrar Vuelo</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal Eliminar Vuelo -->
    <div class="modal fade" id="modalEliminarVuelo" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Eliminar Vuelo</h5>
                    <button type="button" class="close" data-dismiss="modal">
                        <span>&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div id="errorEliminarVuelo" class="alert alert-danger" style="display: none;"></div>
                    <input type="hidden" id="eliminarVueloId">
                    <p>¿Está seguro que desea eliminar este vuelo?</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-danger" onclick="confirmarEliminacionVuelo()">Confirmar Eliminación</button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
